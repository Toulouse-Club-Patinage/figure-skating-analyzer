# === Required ===
SECRET_KEY=change-me-to-a-random-string

# === First-run bootstrap (optional — if unset, use /setup UI) ===
# ADMIN_EMAIL=admin@example.com
# ADMIN_PASSWORD=changeme123
# CLUB_NAME=My Club
# CLUB_SHORT=MC

# === Database (default: SQLite in /data) ===
# DATABASE_URL=sqlite+aiosqlite:////data/skating.db
# DATABASE_URL=postgresql+asyncpg://user:pass@host/dbname

# === Google OAuth (optional — button hidden if unset) ===
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# === Serveur MCP (Claude) — optionnel : désactivé si non défini (issuer localhost) ===
# URL publique EXACTE de l'instance, sans slash final, en HTTPS obligatoire hors
# localhost (sert d'issuer OAuth et de base à l'URL MCP {PUBLIC_BASE_URL}/mcp).
# PUBLIC_BASE_URL=https://skatelab.toulouseclubpatinage.com

# === Misc ===
# PDF_DIR=/data/pdfs
# ALLOWED_ORIGINS=http://localhost:5173
# SECURE_COOKIES=true

# === Email notifications (optional — disabled if SMTP_HOST is empty) ===
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# SMTP_FROM=noreply@yourclub.com
